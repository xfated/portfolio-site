import React, { Component } from 'react';
import Timeline from './Timeline';

const AboutMe = (props) => {
    return(
        <React.Fragment>
            <Timeline />
        </React.Fragment>
    );
}

export default AboutMe;