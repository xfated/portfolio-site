import React, { Component } from 'react';
import Timeline from './TimelineComponent';

const AboutMe = (props) => {
    return(
        <React.Fragment>
            <Timeline />
        </React.Fragment>
    );
}

export default AboutMe;